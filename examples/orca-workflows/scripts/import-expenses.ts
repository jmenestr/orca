#!/usr/bin/env node

// Manual prep script: import CSV rows into Notion as Uploaded status.
// v1 stays outside Orca UI; run before `@task:verify-expenses`.

process.stdout.write('import-expenses stub: wire your CSV importer here\n')
